\documentclass[aspectratio=169,11pt]{beamer}

\usepackage{amsmath,amssymb,bm}
\usepackage{booktabs,tabularx,array}
\usepackage{graphicx}
\usepackage{xcolor}

\graphicspath{{docs/presentations/afterSRC-samurai-detector-review/figures/}{../afterSRC-samurai-detector-review/figures/}}

\definecolor{Navy}{HTML}{17324D}
\definecolor{Teal}{HTML}{128C8C}
\definecolor{Gold}{HTML}{D49A2A}
\definecolor{Ink}{HTML}{202A33}
\definecolor{Mist}{HTML}{EEF3F5}
\definecolor{SoftTeal}{HTML}{DDEEEE}
\definecolor{Muted}{HTML}{63717C}

\setbeamercolor{normal text}{fg=Ink,bg=white}
\setbeamercolor{frametitle}{fg=Navy,bg=white}
\setbeamercolor{title}{fg=Navy}
\setbeamercolor{subtitle}{fg=Teal}
\setbeamercolor{structure}{fg=Teal}
\setbeamercolor{alerted text}{fg=Gold}
\setbeamercolor{block title}{fg=white,bg=Navy}
\setbeamercolor{block body}{fg=Ink,bg=Mist}
\setbeamercolor{block title alerted}{fg=white,bg=Teal}
\setbeamercolor{block body alerted}{fg=Ink,bg=SoftTeal}

\setbeamerfont{title}{size=\Huge,series=\bfseries}
\setbeamerfont{subtitle}{size=\Large}
\setbeamerfont{frametitle}{size=\LARGE,series=\bfseries}
\setbeamerfont{block title}{size=\large,series=\bfseries}

\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{frametitle}{\vspace{0.30em}\insertframetitle\par\vspace{0.12em}{\color{Teal}\rule{\textwidth}{1.2pt}}}
\setbeamertemplate{footline}{\leavevmode\hbox to\paperwidth{\hspace{0.65cm}{\color{Muted}\scriptsize\strut SAMURAI73 two-station polarimetry}\hfill{\color{Muted}\scriptsize\strut\insertframenumber/\inserttotalframenumber}\hspace{0.65cm}}\vspace{0.28em}}

\newcommand{\vct}[1]{\bm{#1}}
\newcommand{\Tr}{\operatorname{Tr}}
\newcommand{\Tens}{\bm P^{(2)}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcommand{\conclusion}[1]{\begin{beamercolorbox}[rounded=true,sep=0.55em,wd=\textwidth]{block body alerted}\centering\bfseries\color{Navy}#1\end{beamercolorbox}}

\title{Why SAMURAI73 Needs Two Polarimeters}
\subtitle{Full tensor reconstruction and locating \texorpdfstring{$p_{yy}$}{pyy} loss}
\author{Baiting Tian · Tsinghua University}
\date{3 September 2026}

\begin{document}

\begin{frame}[plain]
  \vspace{1.00cm}
  {\usebeamerfont{title}\color{Navy}\inserttitle\par}
  \vspace{0.45cm}
  {\usebeamerfont{subtitle}\color{Teal}\insertsubtitle\par}
  \vfill
  {\large\color{Ink}\insertauthor\par}
  \vspace{0.25cm}
  {\color{Muted}\insertdate\par}
  \note{[Sources] SAMURAI73 polarized-deuteron IVR proposal; SAMURAI73 polarimeter-location request.}
\end{frame}

% [EN] Open with the operational beam mode and the two locations that bracket the relevant transport. / [CN] 先说明实际束流模式，再用两个位置夹住需要诊断的输运区段。
\begin{frame}{IVR needs the tensor state delivered to the target}
  \centering
  \includegraphics[trim=0 215 0 0,clip,width=0.92\textwidth,height=0.47\textheight,keepaspectratio]{samurai-location-map.png}

  \vspace{0.20em}
  \begin{columns}[T,onlytextwidth]
    \column{0.31\textwidth}
      \textbf{First IVR run}\\[0.15em]
      Start with vertical $p_{yy}$; longitudinal $p_{zz}$ is harder to prepare.
    \column{0.31\textwidth}
      \textbf{After SRC}\\[0.15em]
      What polarization leaves the accelerator chain?
    \column{0.31\textwidth}
      \textbf{After BigRIPS}\\[0.15em]
      What polarization reaches SAMURAI73?
  \end{columns}

  % [EN] State the full-state reason for the two locations before entering the density-matrix derivation. / [CN] 在进入密度矩阵推导前，先点明设置两个位置是为了完整确定状态。
  \vspace{0.25em}
  {\small\color{Muted}Two stations are needed to determine all degrees of freedom of the spin-density matrix.\par}

  \note{[Sources] RIKEN BigRIPS Technical Information configuration map; SAMURAI73 IVR proposal; user-confirmed plan to begin with pyy because pzz is difficult to prepare.}
\end{frame}

% [EN] Count the full state, tensor-only state, and prepared fixed-axis state without conflating them. / [CN] 分开计算一般态、纯张量态和固定轴制备态的自由度。
\begin{frame}{Spin-1: 8 general degrees, 5 tensor, 1 prepared}
  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      For $w_a\geq0$ and $\sum_a w_a=1$,
      \[
        \rho=\sum_a w_a|\psi_a\rangle\langle\psi_a|.
      \]
      A normalized $3\times3$ Hermitian $\rho$ has
      \[
        \boxed{8=3_{\rm vector}+5_{\rm tensor}}
      \]
      independent real parameters.
    \column{0.49\textwidth}
      An axial tensor prepared about $\hat{\vct n}$ is
      \[
        \Tens_0(q,\hat{\vct n})
        =\frac q2\left(3\hat{\vct n}\hat{\vct n}^{\mathsf T}-I\right).
      \]
      \[
        \hat{\vct n}=\hat{\vct y}:
        \quad \Tens_0=\frac{p_{yy}}2\operatorname{diag}(-1,2,-1),
      \]
      \vspace{-0.45em}
      \[
        \hat{\vct n}=\hat{\vct z}:
        \quad \Tens_0=\frac{p_{zz}}2\operatorname{diag}(-1,-1,2).
      \]
  \end{columns}

  \vspace{0.45em}
  \conclusion{Tensor only: 5 degrees. Fixed preparation axis: one amplitude $q$.}
  \note{[Sources] Ohlsen spin-1 density-matrix convention; DPOLAR Cartesian density-matrix parameterization. pyy and pzz are two preparation axes of the same axial-tensor family.}
\end{frame}

% [EN] Separate physical spin evolution from the passive change to the downstream beam basis, and state the rank-preserving scope of linear BMT. / [CN] 将真实自旋演化与下游束流坐标系的被动变换分开，并明确线性 BMT 保持张量阶数的适用范围。
\begin{frame}{Linear BMT preserves vector and tensor ranks}
  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      \textbf{1. Magnetic field rotates the spin in the lab}
      \[
        \rho_{\mathrm{lab},f}
        =U_s\rho_0U_s^\dagger,
        \qquad R_s\in SO(3).
      \]
      Thomas--BMT determines $R_s$ along the trajectory.

    \column{0.47\textwidth}
      \textbf{2. The downstream beam defines new axes}
      \[
        \rho_{b,f}
        =U_o^\dagger\rho_{\mathrm{lab},f}U_o.
      \]
      $R_o$ follows the transported momentum and defines the local $x_b,y_b,z_b$ frame.
  \end{columns}

  \vspace{0.30em}
  \[
    \boxed{R_{\mathrm{rel}}=R_o^{\mathsf T}R_s},
    \qquad
    \vct p_{b,f}=R_{\mathrm{rel}}\vct p_0,
    \qquad
    \Tens_{b,f}=R_{\mathrm{rel}}\Tens_0R_{\mathrm{rel}}^{\mathsf T}.
  \]

  \vspace{0.10em}
  \conclusion{Lowest order: a common rotation moves the polarization axis, but vector and tensor ranks remain separate.}
  \note{[Sources] Thomas-BMT equation; Ohlsen spin-1 irreducible multipoles; DPOLAR local beam-frame transport convention. The first transformation is physical evolution; the second only re-expresses the same state in the downstream beam basis.}
\end{frame}

% [EN] Show that a distribution of ordinary BMT rotations broadens the tensor sector without generating vector polarization. / [CN] 说明普通 BMT 旋转的分布只会扩展张量子空间，而不会产生矢量极化。
\begin{frame}{BMT field spread cannot convert tensor to vector}
  Each phase-space point $\xi=(x,x',y,y',\Delta p/p,\ldots)$ has its own calculated rotation:
  \[
    \vct p_f(\xi)=R_\xi\vct p_0,
    \qquad
    \Tens_f(\xi)=R_\xi\Tens_0R_\xi^{\mathsf T}.
  \]

  The polarimeter measures the ensemble average:
  \[
    \overline{\vct p}_f=\int d\xi\,f(\xi)R_\xi\vct p_0,
    \qquad
    \overline{\Tens}_f=\int d\xi\,f(\xi)R_\xi\Tens_0R_\xi^{\mathsf T}.
  \]

  \vspace{0.10em}
  \begin{columns}[T,onlytextwidth]
    \column{0.46\textwidth}
      If $\vct p_0=0$, then exactly
      \[
        \overline{\vct p}_f=0.
      \]
    \column{0.48\textwidth}
      The averaged tensor can become biaxial and require all
      \[
        \boxed{5\ \text{tensor components}}.
      \]
  \end{columns}

  \vspace{0.20em}
  \conclusion{Different $\vct B(\xi)$ under linear BMT can open up to 5 tensor degrees; it cannot give tensor$\rightarrow$vector conversion.}
  \note{[Sources] Wigner rotation law for spin-1 irreducible multipoles; DPOLAR phase-space-averaged BMT transport. A convex average of rank-preserving rotations remains rank preserving.}
\end{frame}

% [EN] Add the distinct higher-order mechanisms that can couple vector and tensor multipoles and therefore open the full density matrix. / [CN] 单独加入能够耦合矢量与张量多极矩、从而打开完整密度矩阵的高阶机制。
\begin{frame}{Rank mixing opens all 8 state parameters}
  Tensor--vector conversion requires spin dynamics beyond the linear BMT term. For example,
  \[
    H_{\rm spin}
    =-\mu\,\vct S\!\cdot\!\vct B
     -\beta_T\bigl(\vct S\!\cdot\!\vct B\bigr)^2+\cdots,
    \qquad
    \overline\rho_f=\int d\xi\,f(\xi)\,\mathcal E_\xi[\rho_0].
  \]

  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      \textbf{Coherent rank mixing}

      Tensor magnetic/electric polarizability or other quadratic spin couplings can generate
      \[
        \vct p_0=0,\ \Tens_0\neq0
        \quad\longrightarrow\quad
        \vct p_f\neq0.
      \]
    \column{0.47\textwidth}
      \textbf{Open or selective transport}

      Spin-dependent loss, material interactions, collimation, and acceptance can change both ranks and the density-matrix eigenvalues.
  \end{columns}

  \vspace{0.35em}
  \conclusion{Once rank mixing is allowed, the fit must allow the full $8=3_{\rm vector}+5_{\rm tensor}$ parameters.}
  \note{[Sources] A. J. Silenko, Phys. Rev. C 77, 021001 (2008), arXiv:0711.2390; general spin-1 density-matrix evolution. Silenko gives an explicit initially tensor-polarized deuteron beam acquiring vector polarization through tensor magnetic polarizability.}
\end{frame}

% [EN] Contrast nominal-mode monitoring with reconstruction of a full state after rank-preserving or rank-mixing transport. / [CN] 区分名义模式监测与在保持或混合阶数的输运后重建完整状态。
\begin{frame}{Two stations can recover all 8 degrees}
  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      \textbf{Nominal $p_{yy}$ monitoring}
      \[
        \frac{N_L+N_R-N_U-N_D}
        {N_L+N_R+N_U+N_D}
        \quad\Longrightarrow\quad p_{yy}.
      \]
      One station measures the amplitude if the beam is assumed to remain a pure $p_{yy}$ state.
    \column{0.47\textwidth}
      \textbf{General-state test}

      One L/R/U/D station is blind to tensor $p_{xy}$ and vector $p_z$.

      \vspace{0.35em}
      A complementary second view rotates these directions into measured combinations.
  \end{columns}

  \vspace{0.45em}
  \renewcommand{\arraystretch}{1.05}
  \begin{tabularx}{\textwidth}{Y c c}
    \toprule
    Calibrated multi-angle measurement & tensor state & full density matrix \\
    \midrule
    one station & $4/5$ & $6/8$ \\
    two stations with a characterized complementary transfer map & $\alert{5/5}$ & $\alert{8/8}$ \\
    \bottomrule
  \end{tabularx}

  \note{[Sources] DPOLAR multi-angle L/R/U/D response and joint-rank analysis. A single tensor station has the exact pxy blind direction; the full response also omits pz.}
\end{frame}

% [EN] Diagnose a lower pyy using tensor magnitude, generated vector polarization, and full density-matrix purity before assigning a location. / [CN] 用张量幅度、产生的矢量极化和完整密度矩阵纯度诊断 pyy 下降，再判断其发生位置。
\begin{frame}{Full $\rho$ identifies what changed and where}
  A lower $p_{yy}$ component has three distinct interpretations. Use
  \[
    Q_T=\sqrt{\frac{2}{3}\Tr[(\Tens)^2]},
    \qquad V=|\vct p|,
    \qquad \Pi=\Tr(\rho^2).
  \]

  \renewcommand{\arraystretch}{1.05}
  \begin{tabularx}{\textwidth}{>{\bfseries}Y Y}
    measured change & physical interpretation \\
    \midrule
    $Q_T$ unchanged, $V=0$, tensor axis changes & ordinary coherent rotation \\
    $Q_T$ changes, $V$ appears, $\Pi$ unchanged & coherent tensor--vector conversion \\
    $\Pi$ decreases & incoherent ensemble depolarization \\
  \end{tabularx}

  \vspace{0.20em}
  \conclusion{Changed already after SRC $\Rightarrow$ upstream. Changed only after BigRIPS $\Rightarrow$ between the stations.}
  \note{[Sources] Density-matrix purity under unitary evolution; tensor-multipole invariants under ordinary rotations; SAMURAI73 two-station loss-localization requirement.}
\end{frame}

% [EN] End with the concrete coordination decision needed from the RIKEN participants. / [CN] 以需要 RIKEN 参会者确认的具体协调事项收束报告。
\begin{frame}{Who is the contact for an after-SRC polarimeter?}
  \vspace{0.35em}
  {\Large\bfseries\color{Navy}Could a polarimeter be installed just downstream of SRC?\par}

  \vspace{0.75em}
  {\large Who should we contact to discuss\par}
  \vspace{0.30em}
  \begin{itemize}
    \setlength\itemsep{0.45em}
    \item the available beam-line location and longitudinal space;
    \item vacuum, mechanical, electrical, and detector interfaces;
    \item beam optics, operation, radiation safety, and installation constraints?
  \end{itemize}

  \vspace{0.70em}
  \conclusion{Requested next step: identify the responsible beam-line contact and arrange an interface discussion.}
  \note{[Sources] SAMURAI73 polarimeter-location request.}
\end{frame}

\end{document}
