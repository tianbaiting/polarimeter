schema_version: 1
module: infrontofSamuraiMag
intent:
  description: "Locked legacy-center preview baseline: HVV plate placement/size frozen for iterative tuning, detector bases direct-mounted to plates, plate cuts disabled, overlap checks skipped."
build:
  config: config/profiles/legacy_center_preview_locked.yaml
  overrides: []
  doc_name: infrontofSamuraiMag
  mode: build
validation:
  strict: false
  angle_tolerance_deg: 0.05
  radius_tolerance_mm: 0.2
output:
  output_dir: .
  basename: infrontofSamuraiMag
  formats: [fcstd, step]
artifacts:
  report_json: infrontofSamuraiMag.validation_report.json
