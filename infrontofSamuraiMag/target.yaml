schema_version: 1
module: infrontofSamuraiMag
intent:
  description: "Implement BLP v1 concept-level mechanics (chamber/H+V+V plates/detector clamp-target ladder-stand) with strict subsystem validation."
build:
  config: config/default_infront.yaml
  overrides: []
  doc_name: infrontofSamuraiMag
  mode: build
validation:
  strict: true
  angle_tolerance_deg: 0.05
  radius_tolerance_mm: 0.2
output:
  output_dir: .
  basename: infrontofSamuraiMag
  formats: [fcstd, step]
artifacts:
  report_json: infrontofSamuraiMag.validation_report.json
